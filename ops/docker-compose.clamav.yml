# ClamAV sidecar for EICAR drills. Production mandate is CLAMAV_REQUIRED=true + CLAMAV_HOST.
services:
  clamav:
    image: clamav/clamav:1.4
    ports:
      - "127.0.0.1:3310:3310"
    healthcheck:
      test: ["CMD-SHELL", "clamdscan --ping 1 || exit 1"]
      interval: 15s
      retries: 20
